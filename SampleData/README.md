# Sample Data

These files contain small artificial alignments for trying ApuSeq and for App Store review notes.

- `demo.fasta` - FASTA nucleotide alignment with gaps, mixed names, and a short trailing-gap case
- `demo.clustal` - CLUSTAL alignment using the same general sequence set

The sequences are synthetic and are not derived from biological samples.
